{
  "name": "rxiver-gloss",
  "version": "0.2.0",
  "description": "The rxiver Chrome companion for explanations, PDF reading, history, and collection export.",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "node build.mjs",
    "package": "npm run test && npm run build && node package.mjs",
    "test": "vitest run"
  },
  "dependencies": {
    "pdfjs-dist": "^4.7.76"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "@types/node": "^22.10.2",
    "esbuild": "^0.24.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
